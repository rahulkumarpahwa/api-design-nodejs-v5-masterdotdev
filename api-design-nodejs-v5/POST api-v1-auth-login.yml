info:
  name: POST api-v1-auth-login
  type: http
  seq: 1

http:
  method: POST
  url: '{{url}}/api/v1/auth/login'
  body:
    type: json
    data: |-
      {
        "email": "demo@demo.com",
        "password": "password@demo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​